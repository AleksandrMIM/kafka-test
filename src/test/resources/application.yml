spring:
  main:
    web-application-type: none
  kafka:
    topic: testTopic
    partitions: 1
    consumer:
      bootstrap-servers: localhost:9092
      group-id: batch
      fetch-max-wait: 1
      max-poll-records: 1